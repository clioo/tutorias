export interface Clase{
    title:string,
    idGrupo: string,
    descripcion:string,
    materia:string,
    estado:string, //a = terminado, b=en curso, c= sin empezar
    entrega:string,
    start:Date,
    end:Date
}